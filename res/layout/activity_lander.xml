<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".LanderActivity" >

    <TextView
        android:id="@+id/lander_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/hello_world" />
    
    <!-- This button is only used for accessing the address screen. 
         Should be replaced when the landing page is fully implemented -->
    <Button
        android:id="@+id/addr_screen_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/lander_label"
        android:layout_centerHorizontal="true"
        android:text="Addresses" />
     <!-- End address button -->
</RelativeLayout>
