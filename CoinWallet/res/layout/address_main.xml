<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >
    

    <Button
        android:id="@+id/add_new_addr_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/add_new_addr" />
    
    <ListView
        android:id="@+id/address_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/add_new_addr_btn" >
    </ListView>

    <Button
        android:id="@+id/archived_addr_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:text="@string/show_archived" />

    <TextView
        android:id="@+id/balance"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/add_new_addr_btn"
        android:layout_alignParentTop="true"
        android:layout_toLeftOf="@+id/archived_addr_btn"
        android:layout_toRightOf="@+id/add_new_addr_btn"
        android:clickable="true"
        android:onClick="balanceClicked"
        android:gravity="center"
        android:text="Balance:" />

</RelativeLayout>
